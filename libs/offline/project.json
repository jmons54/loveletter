{
  "name": "offline",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "offline/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../coverage/offline"
      }
    }
  }
}
